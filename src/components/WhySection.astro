<section class="why-section" id="dlaczego">
	<div class="container why-container">
		<div class="why-heading">
			<p class="eyebrow">O naszej inicjatywie</p>

			<h2>Dlaczego powstał Komitet?</h2>

			<p class="why-lead">
				Powstaliśmy, ponieważ bezpieczeństwo leczenia dzieci wymaga
				transparentności, dostępu do rzetelnych informacji oraz wspólnego,
				odpowiedzialnego działania.
			</p>
		</div>

		<div class="why-content">
			<div class="why-text">
				<p>
					Komitet Rodziców i Pacjentów GCZD na rzecz Bezpiecznej Onkologii
					Dziecięcej powstał z inicjatywy rodziców dzieci leczonych
					onkologicznie oraz pacjentów.
				</p>

				<p>
					Naszym celem jest przedstawianie faktów, publikowanie dokumentów
					i wspieranie działań prowadzących do zapewnienia dzieciom dostępu
					do kompleksowej, bezpiecznej i odpowiedzialnie zorganizowanej
					opieki.
				</p>

				<a class="why-link" href="/o-nas">
					Poznaj historię Komitetu
					<span aria-hidden="true">→</span>
				</a>
			</div>

			<div class="why-pillars">
				<article class="pillar-card">
					<div class="pillar-number">01</div>
					<h3>Transparentność</h3>
					<p>
						Publikujemy dokumenty, odpowiedzi instytucji oraz informacje
						dotyczące podejmowanych działań.
					</p>
				</article>

				<article class="pillar-card">
					<div class="pillar-number">02</div>
					<h3>Rzetelność</h3>
					<p>
						Opieramy się na faktach, dokumentach i sprawdzonych źródłach,
						unikając niepotwierdzonych informacji.
					</p>
				</article>

				<article class="pillar-card">
					<div class="pillar-number">03</div>
					<h3>Współpraca</h3>
					<p>
						Łączymy rodziców, pacjentów, ekspertów i media wokół wspólnego
						celu — bezpieczeństwa dzieci.
					</p>
				</article>
			</div>
		</div>
	</div>
</section>

<style>
	.why-section {
		padding: 112px 0;
		background:
			radial-gradient(
				circle at 85% 15%,
				rgba(12, 157, 165, 0.08),
				transparent 30%
			),
			#ffffff;
	}

	.why-container {
		display: grid;
		gap: 70px;
	}

	.why-heading {
		max-width: 800px;
	}

	.why-heading h2 {
		max-width: 700px;
		margin: 0;
		font-size: clamp(2.35rem, 4vw, 4rem);
		line-height: 1.05;
		letter-spacing: -0.045em;
		color: var(--navy);
	}

	.why-lead {
		max-width: 720px;
		margin: 24px 0 0;
		font-size: clamp(1.12rem, 1.5vw, 1.35rem);
		line-height: 1.65;
		color: var(--muted);
	}

	.why-content {
		display: grid;
		grid-template-columns: minmax(260px, 0.72fr) minmax(0, 1.28fr);
		gap: 80px;
		align-items: start;
	}

	.why-text {
		position: sticky;
		top: 110px;
	}

	.why-text p {
		margin: 0;
		font-size: 1.05rem;
		line-height: 1.75;
		color: var(--muted);
	}

	.why-text p + p {
		margin-top: 20px;
	}

	.why-link {
		display: inline-flex;
		align-items: center;
		gap: 10px;
		margin-top: 30px;
		font-weight: 700;
		color: var(--teal-dark);
		text-decoration: none;
		transition:
			gap 180ms ease,
			color 180ms ease;
	}

	.why-link:hover {
		gap: 14px;
		color: var(--navy);
	}

	.why-pillars {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 18px;
	}

	.pillar-card {
		min-height: 280px;
		padding: 30px;
		border: 1px solid rgba(10, 55, 101, 0.11);
		border-radius: 24px;
		background: rgba(255, 255, 255, 0.82);
		box-shadow: 0 18px 50px rgba(17, 56, 91, 0.07);
		backdrop-filter: blur(10px);
		transition:
			transform 180ms ease,
			border-color 180ms ease,
			box-shadow 180ms ease;
	}

	.pillar-card:hover {
		transform: translateY(-5px);
		border-color: rgba(12, 157, 165, 0.28);
		box-shadow: 0 24px 65px rgba(17, 56, 91, 0.11);
	}

	.pillar-number {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 44px;
		height: 44px;
		border-radius: 14px;
		background: rgba(12, 157, 165, 0.1);
		font-size: 0.78rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		color: var(--teal-dark);
	}

	.pillar-card h3 {
		margin: 42px 0 12px;
		font-size: 1.32rem;
		line-height: 1.2;
		letter-spacing: -0.025em;
		color: var(--navy);
	}

	.pillar-card p {
		margin: 0;
		font-size: 0.98rem;
		line-height: 1.68;
		color: var(--muted);
	}

	@media (max-width: 1050px) {
		.why-content {
			grid-template-columns: 1fr;
			gap: 48px;
		}

		.why-text {
			position: static;
			max-width: 720px;
		}
	}

	@media (max-width: 820px) {
		.why-section {
			padding: 88px 0;
		}

		.why-container {
			gap: 52px;
		}

		.why-pillars {
			grid-template-columns: 1fr;
		}

		.pillar-card {
			min-height: auto;
		}

		.pillar-card h3 {
			margin-top: 28px;
		}
	}

	@media (max-width: 600px) {
		.why-section {
			padding: 72px 0;
		}

		.why-heading h2 {
			font-size: clamp(2.05rem, 10vw, 3rem);
		}

		.why-lead {
			font-size: 1.08rem;
		}

		.pillar-card {
			padding: 25px;
			border-radius: 20px;
		}
	}
</style>