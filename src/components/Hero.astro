<section class="hero">
	<div class="hero-glow" aria-hidden="true"></div>

	<div class="container hero-inner">
		<div class="hero-copy">
			<p class="eyebrow">Głos rodziców i pacjentów</p>

			<h1>
				Wspólnie walczymy
				<br />
				o bezpieczną i kompleksową
				<span>onkologię dziecięcą.</span>
			</h1>

			<p class="hero-description">
				Działamy na rzecz przywrócenia dzieciom na Śląsku dostępu
				do kompleksowej opieki onkologicznej i hematologicznej,
				opartej na doświadczeniu, bezpieczeństwie i współpracy specjalistów.
			</p>

			<div class="hero-actions">
				<a class="button button-primary" href="/o-nas">
					Poznaj naszą inicjatywę
				</a>

				<a class="button button-secondary" href="/dokumenty">
					Zobacz dokumenty
				</a>
			</div>
		</div>

		<div class="hero-visual" aria-hidden="true">
			<div class="hero-sky"></div>

			<img
				class="hero-boy"
				src="/images/hero-boy.png"
				alt=""
				width="650"
				height="760"
				loading="eager"
				fetchpriority="high"
			/>

			<img
				class="hero-star"
				src="/images/hero-star.svg"
				alt=""
				width="92"
				height="92"
				loading="eager"
			/>

			<aside class="hero-quote">
				<span class="quote-mark">“</span>

				<blockquote>
					Państwa-Strony uznają prawo dziecka do korzystania
					z możliwie najwyższego poziomu ochrony zdrowia.
				</blockquote>

				<div class="quote-divider"></div>

				<p>
					Konwencja o prawach dziecka<br />
					art. 24 ust. 1
				</p>
			</aside>
		</div>
	</div>

	<div class="hero-wave" aria-hidden="true"></div>
</section>

<style>
	.hero {
		position: relative;
		overflow: hidden;
		background:
			radial-gradient(
				circle at 10% 40%,
				rgba(7, 157, 167, 0.1),
				transparent 34%
			),
			#ffffff;
	}

	.hero-inner {
		position: relative;
		z-index: 2;
		display: grid;
		grid-template-columns: minmax(0, 1.05fr) minmax(480px, 0.95fr);
		align-items: center;
		min-height: 650px;
		padding-top: 58px;
		padding-bottom: 105px;
	}

	.hero-copy {
		position: relative;
		z-index: 5;
		max-width: 620px;
		padding-right: 28px;
	}

	.hero-copy h1 {
		margin: 0;
		max-width: 620px;
		font-size: clamp(2.7rem, 4.1vw, 4.55rem);
		line-height: 1.04;
		letter-spacing: -0.048em;
		color: var(--navy);
	}

	.hero-copy h1 span {
		display: block;
		color: var(--teal-dark);
	}

	.hero-description {
		max-width: 570px;
		margin: 25px 0 0;
		font-size: clamp(1.02rem, 1.25vw, 1.18rem);
		line-height: 1.66;
		color: var(--muted);
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 15px;
		margin-top: 34px;
	}

	.hero-visual {
		position: absolute;
		z-index: 1;
		top: 0;
		right: max(0px, calc((100vw - 1180px) / 2));
		bottom: 0;
		width: min(55vw, 690px);
		min-width: 540px;
		overflow: hidden;
	}

	.hero-sky {
		position: absolute;
		inset: 0;
		background:
			linear-gradient(
				90deg,
				#ffffff 0%,
				rgba(255, 255, 255, 0.92) 11%,
				rgba(255, 255, 255, 0.43) 27%,
				rgba(255, 255, 255, 0.04) 48%,
				rgba(255, 255, 255, 0) 68%
			),
			url("/images/hero-sky.webp") center / cover no-repeat;
	}

	.hero-glow {
		position: absolute;
		z-index: 1;
		top: 12%;
		right: 22%;
		width: 390px;
		height: 390px;
		border-radius: 50%;
		background: rgba(29, 157, 176, 0.12);
		filter: blur(80px);
		pointer-events: none;
	}

	.hero-boy {
		position: absolute;
		z-index: 2;
		left: 20%;
		bottom: 10px;
		width: auto;
		height: min(76%, 500px);
		max-width: none;
		object-fit: contain;
		pointer-events: none;
		user-select: none;
	}

	.hero-star {
		position: absolute;
		z-index: 3;
		top: 72px;
		left: 47%;
		width: 76px;
		height: 76px;
		pointer-events: none;
		filter: drop-shadow(0 0 18px rgba(245, 190, 65, 0.48));
	}

	.hero-quote {
		position: absolute;
		z-index: 4;
		top: 50%;
		right: 8px;
		width: 245px;
		padding: 18px 0 18px 24px;
		border-left: 1px solid rgba(255, 255, 255, 0.52);
		color: #ffffff;
		text-shadow: 0 2px 15px rgba(2, 28, 67, 0.62);
		transform: translateY(-43%);
	}

	.quote-mark {
		display: block;
		height: 32px;
		font-family: Georgia, serif;
		font-size: 4.2rem;
		line-height: 1;
		color: #45d0d5;
	}

	.hero-quote blockquote {
		margin: 9px 0 18px;
		font-family: Georgia, "Times New Roman", serif;
		font-size: 1.17rem;
		line-height: 1.43;
	}

	.quote-divider {
		width: 40px;
		height: 3px;
		margin-bottom: 14px;
		background: var(--gold);
	}

	.hero-quote p {
		margin: 0;
		font-size: 0.85rem;
		line-height: 1.5;
		color: #65e0e2;
	}

	.hero-wave {
		position: absolute;
		z-index: 7;
		left: -5%;
		right: -5%;
		bottom: -68px;
		height: 112px;
		border-radius: 50% 50% 0 0;
		border-top: 5px solid var(--teal);
		background: var(--background);
		transform: rotate(-0.35deg);
	}

	@media (max-width: 1100px) {
		.hero-inner {
			grid-template-columns: minmax(0, 1fr) 420px;
		}

		.hero-visual {
			right: 0;
			width: 53%;
			min-width: 500px;
		}

		.hero-boy {
			left: 14%;
			height: 455px;
		}

		.hero-star {
			left: 42%;
		}

		.hero-quote {
			width: 220px;
			right: 12px;
		}
	}

	@media (max-width: 940px) {
		.hero-inner {
			display: block;
			min-height: auto;
			padding-top: 52px;
			padding-bottom: 420px;
		}

		.hero-copy {
			max-width: 720px;
			padding-right: 0;
		}

		.hero-visual {
			top: auto;
			right: 0;
			bottom: 0;
			width: 100%;
			height: 390px;
			min-width: 0;
		}

		.hero-sky {
			background:
				linear-gradient(
					180deg,
					#ffffff 0%,
					rgba(255, 255, 255, 0.7) 16%,
					rgba(255, 255, 255, 0) 50%
				),
				url("/images/hero-sky.webp") 65% center / cover no-repeat;
		}

		.hero-boy {
			left: auto;
			right: 12%;
			bottom: 0;
			height: 350px;
		}

		.hero-star {
			top: 26px;
			left: auto;
			right: 20%;
			width: 64px;
			height: 64px;
		}

		.hero-quote {
			display: none;
		}
	}

	@media (max-width: 600px) {
		.hero-inner {
			padding-top: 40px;
			padding-bottom: 315px;
		}

		.hero-copy h1 {
			font-size: clamp(2.2rem, 10.5vw, 3.2rem);
			letter-spacing: -0.04em;
		}

		.hero-description {
			font-size: 1rem;
			line-height: 1.58;
		}

		.hero-actions {
			flex-direction: column;
			margin-top: 28px;
		}

		.hero-actions .button {
			width: 100%;
		}

		.hero-visual {
			height: 290px;
		}

		.hero-boy {
			right: -1%;
			height: 270px;
		}

		.hero-star {
			top: 16px;
			right: 14%;
			width: 52px;
			height: 52px;
		}

		.hero-wave {
			bottom: -72px;
		}
	}
</style>